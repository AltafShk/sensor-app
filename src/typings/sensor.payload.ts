// Comment: There can be other attributes in the payload here if they are known to us.
// Since the prompt kept the payload abstract, so have I in the implementation here.
export interface SensorPayload {
  id: string;
}
